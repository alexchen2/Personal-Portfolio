// Stores contexts used within the app
import { createContext } from "react";

const HeightRefContext = createContext(null);
const ParallaxContext = createContext(null);

export {HeightRefContext, ParallaxContext};